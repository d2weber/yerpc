[package]
name = "c-test"
version = "0.0.0"
edition = "2021"
publish = false

[build-dependencies]
cc = "1"
yerpc = { path = "../yerpc" }
typescript-type-def = { version = "0.5.13", features = ["json_value"] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
